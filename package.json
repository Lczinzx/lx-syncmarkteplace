{
  "name": "lx-sync-marketplace-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && wrangler deploy",
    "preview": "vite preview",
    "test": "node --test tests/**/*.test.js"
  },
  "devDependencies": {
    "vite": "^5.4.21",
    "wrangler": "^4.118.0"
  }
}
